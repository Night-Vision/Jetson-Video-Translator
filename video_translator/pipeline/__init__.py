# Intentionally empty. Stage classes are imported explicitly by callers
# (main.py). Eager imports here caused a RuntimeWarning + double-import when
# the transcriber worker runs `python -m video_translator.pipeline.transcriber`.
